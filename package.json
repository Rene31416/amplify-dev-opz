{
  "name": "amplify-dev-opz",
  "version": "0.1.0",
  "bin": {
    "amplify-dev-opz": "bin/amplify-dev-opz.js"
  },
  "scripts": {
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "build": "esbuild ./src/**/** --bundle --platform=node --target=node18 --outdir=dist && esbuild ./src/*.ts --bundle --platform=node --target=node18 --outdir=dist"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.7.9",
    "aws-cdk": "^2.180.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "@aws-cdk/aws-amplify-alpha": "^2.39.1-alpha.0",
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.0.0",
    "esbuild": "^0.25.0"
  }
}
